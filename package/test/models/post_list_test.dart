import 'package:test/test.dart';

import '../utils/load_fixture.dart';
import '../utils/models/post_list.dart';

void main() {
  group('List of Post Tests:', () {
    // test('returns List of Post from json', () {
    //   final posts = PostList.fromJson(loadFixture('post_list'));
    //   print('LIST OF POST\n $posts');

    //   expect(posts, const TypeMatcher<PostList>());
    // });

    // test('returns json string from List of Post', () {
    //   final post = Post.fromJson(loadFixture('post_list'));

    //   final postString = post.toJson();

    //   expect(postString, const TypeMatcher<String>());
    // });
  });
}
